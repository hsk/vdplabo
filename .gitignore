.DS_Store
__pycache__/
*.pyc

# Z80 build outputs (rebuild with `make`; glass-0.6.jar itself is kept tracked)
*.o
*.lis
*.rom
*.log
*.sym

# 見た目確認用に拡大書き出ししたwebm (video_expected.py で再生成できる)
*_view.webm

# capture_openmsx.pyが実行のたびにデバッグ用に書き出すTclスクリプト
sprite/test/asm/out.tcl
