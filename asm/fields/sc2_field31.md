# sc2_field31 ドキュメント

天井は描画せず代わりに０番固定で描画する。
地面だけ描画する。

sc2_field30はsc2_field4を元にしたけど、今回はsc2_field5のさらに高速化したものを改良する。


## 空の描画

空を描く箇所は最終的に必要ないけどsc2_field30と同じで良いのでコピーする。

## 初期状態を作って飛び込む

高速に作ったものを変えずに最初だけ飛ばすには、途中の状態を作って処理の中に飛び込む。
今回のケースだとcolor_table_yに途中の状態を作って飛び込む。
player_y+1回進んだ状態を作って

B = 残り回数（ループカウンタ）
SP = ストライプデータ位置
HL = stripe_color_table2基準
VDPアドレス設定済み
C = 98h

などの状態を丁寧に作って地面だけ描画する。
